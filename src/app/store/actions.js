//External Dependencies

// Store Dependencies

// Util Dependencies

/**
 * Actions creator maps the staet and dispatchers to UI actions
 * @param {function} dispatch -- The Redux store dispatcher
 * @param {Object} state -- The Current redux state
 * @param {Object} options -- Optional object for adding additonal configuraiton to the actions
 */
export const createActions = (dispatch, state, options) => {
  // Create the actions for the interface builder screen
  return {};
};
