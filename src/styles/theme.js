import { red } from '@mui/material/colors';

// Create a theme instance.
const theme = {
  palette: {
    primary: {
      main: '#24243e',
    },
    secondary: {
      main: '#4fc3f7',
    },
  },
};

export default theme;
